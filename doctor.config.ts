import { defineConfig } from "react-doctor/api";

export default defineConfig({
  // This is a preserved pre-React prototype, not an application entry point.
  ignore: { files: ["Euclidean Grid Sequencer.html"] },
});
